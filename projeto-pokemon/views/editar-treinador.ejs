<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Editar Treinador</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container">
    <h1 class="text-primary">Editar Treinador</h1>
    
    <!-- Formulário para editar treinador -->
    <form method="POST" action="/editar-excluir-treinador/<%= treinador.id_treinador %>">
      <!-- Campo de nome -->
      <div class="form-group">
        <label for="nome">Nome:</label>
        <input type="text" id="nome" name="nome" class="form-control" value="<%= treinador.nome %>" required>
      </div>
      
      <!-- Campo de idade -->
      <div class="form-group">
        <label for="idade">Idade:</label>
        <input type="number" id="idade" name="idade" class="form-control" value="<%= treinador.idade %>" required>
      </div>
      
      <!-- Campo de cidade -->
      <div class="form-group">
        <label for="cidade">Cidade:</label>
        <input type="text" id="cidade" name="cidade" class="form-control" value="<%= treinador.cidade %>" required>
      </div>
      
      <!-- Botão de salvar (para editar) -->
      <button type="submit" class="btn btn-primary">Salvar alterações</button>

      
    </form>
  </div>
</body>
</html>
