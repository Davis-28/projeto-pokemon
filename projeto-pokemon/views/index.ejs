<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Projeto Pokémon - Página Inicial</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #ff6a00, #ffcc00, #ff1493);
      font-family: 'Arial', sans-serif;
      color: #fff;
      background-size: 300% 300%;
      animation: gradientBG 10s ease infinite;
    }

    @keyframes gradientBG {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .container {
      background-color: rgba(255, 255, 255, 0.9);
      border-radius: 20px;
      padding: 40px;
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
    }

    h1 {
      color: black;
      font-size: 3rem;
      font-weight: bold;
      text-shadow: 4px 4px 10px rgba(0, 0, 0, 0.4);
      margin-bottom: 30px;
    }

    .lead {
      font-size: 1.6rem;
      font-weight: bold;
      color: #333;
      margin-bottom: 40px;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
    }

    .btn {
      font-size: 1.4rem;
      padding: 20px 35px;
      border-radius: 50px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
      transition: all 0.4s ease-in-out;
      color: white;
      text-transform: uppercase;
    }

    .btn:hover {
      transform: scale(1.1);
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
    }

    .btn-success {
      background-color: #32cd32;
      border-color: #28a745;
    }

    .btn-info {
      background-color: #1e90ff;
      border-color: #007bff;
    }

    .btn-danger {
      background-color: #ff4500;
      border-color: #e63946;
    }

    .btn-success:hover {
      background-color: #28a745;
      border-color: #218838;
    }

    .btn-info:hover {
      background-color: #007bff;
      border-color: #0069d9;
    }

    .btn-danger:hover {
      background-color: #e63946;
      border-color: #d4312f;
    }

    img {
      border-radius: 15px;
      margin-bottom: 30px;
      animation: spin 5s linear infinite;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      0% { transform: rotate(0deg); }
    }

    .btn-container {
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div class="container text-center mt-5">
    
    <img src="https://logodownload.org/wp-content/uploads/2017/08/pokemon-logo-0.png" alt="Logo Pokémon" class="img-fluid mb-4" style="max-width: 400px;">

    <h1>Selecione uma das opções:</h1>
    
    
    
    <div class="d-flex justify-content-center flex-wrap btn-container">
      <a href="/pokemons" class="btn btn-success m-2">Lista de Pokémons</a>
      <a href="/treinadores" class="btn btn-info m-2">Lista de Treinadores</a>
      <a href="/batalhas" class="btn btn-danger m-2">Lista de Batalhas</a>
    </div>
  </div>
</body>
</html>

